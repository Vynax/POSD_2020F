{
    "tasks": [
        {
            "type": "shell",
            "label": "terminal build",
            "command": "/usr/bin/g++",
            "args": [
                "-g",
                "src/main.cpp",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "version": "2.0.0"
}