#include "../src/ellipse.h"

TEST(Shapes, Ellipse)
{
    Ellipse ellipse(3.712, 4);
    //ASSERT_TRUE(12 == rectangle.area());
    //ASSERT_EQ(14.848, ellipse.area());
    //ASSERT_EQ("Rectangle (3.712, 4.000)", ellipse.info());
    ASSERT_TRUE(true);
}